<UserControl x:Class="AxeSoftware.Quest.EditorControls.CompassEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical">
        <TextBlock Name="direction" FontWeight="Bold" TextWrapping="Wrap">North</TextBlock>
        <StackPanel Orientation="Horizontal">
            <Label Name="toLabel">To:</Label>
            <ComboBox Name="to"/>
        </StackPanel>
        <Button Name="create" HorizontalAlignment="Left">Create</Button>
        <Button Name="edit" HorizontalAlignment="Left">Edit</Button>
        <CheckBox Name="chkCorresponding">Also create exit in the other direction</CheckBox>
        <TextBlock Name="corresponding">Exit in the other direction already exists</TextBlock>
        <Button Name="createCorresponding" HorizontalAlignment="Left">Create exit in the other direction</Button>
    </StackPanel>
</UserControl>
