<UserControl x:Class="AxeSoftware.Quest.EditorControls.FindControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" Margin="5,0,0,0">Find:</TextBlock>
        <TextBox Name="txtFind" Grid.Column="1" Margin="3,0,3,0" VerticalContentAlignment="Center" KeyDown="txtFind_KeyDown"/>
        <StackPanel Grid.ColumnSpan="2" HorizontalAlignment="Right" Grid.Column="2" Orientation="Horizontal">
            <Button Name="cmdFind" Margin="2" Padding="3,1,3,1" Click="cmdFind_Click">Find Next</Button>
            <Button Name="cmdClose" Margin="2" Padding="3,1,3,1" Click="cmdClose_Click">Close</Button>
        </StackPanel>
    </Grid>
</UserControl>
