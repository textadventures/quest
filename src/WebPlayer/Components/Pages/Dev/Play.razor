@page "/dev/play"
@attribute [RequiresConfig("Dev:Enabled")]
@using QuestViva.Common
@using QuestViva.PlayerCore
@rendermode InteractiveServer

<Game GameDataProvider="@GetGameDataProvider()" ResourceProvider="@GetResourceProvider()"></Game>

@code {
    [Parameter]
    [SupplyParameterFromQuery]
    public required string File { get; set; }
    
    private IGameDataProvider GetGameDataProvider()
    {
        return new FileGameDataProvider(Path.Combine(Path.GetTempPath(), File));
    }
    
    private IResourceProvider GetResourceProvider()
    {
        return new LocalResourceProvider($"dev.play.{File}");
    }
}