---
title: container_closed
---

The "container\_closed" type is defined in CoreTypes.aslx. It implements a closed container.

By inheriting the "container\_closed" type in an object, it becomes by default [openable](/attributes/open) and [closable](/attributes/close). "Open" and "Close" are added as [displayverbs](/attributes/displayverbs).
