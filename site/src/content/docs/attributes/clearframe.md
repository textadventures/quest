---
title: clearframe
---

"clearframe" is a [boolean](/types/boolean) attribute, which is only relevant if [useframe](/attributes/useframe) is turned on. If clearframe is set to true, if a room does not define a [picture attribute](/attributes/picture), the static picture frame is cleared for that room. If clearframe is set to false, the previous room's image would remain on the screen.
